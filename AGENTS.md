# Agent Instructions

See [CLAUDE.md](CLAUDE.md) — it is the single source of truth for agents working
in this repo (what to read first, the checks, and the rules that are easy to break).

Do not duplicate guidance here; it drifts and doubles the context every agent loads.
