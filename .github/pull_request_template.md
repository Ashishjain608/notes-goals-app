## What and why

<!-- What does this change, and what problem does it solve? Link the issue, e.g. "Closes #12". -->

## How it was checked

- [ ] `npm run typecheck && npm test && cargo test --manifest-path src-tauri/Cargo.toml` pass
- [ ] Tried it in the running app (`npm run tauri:dev`)
- [ ] Screenshots attached for UI changes, in light and dark
- [ ] `CONTEXT.md` and `docs/adr/` updated if this changes a domain rule
